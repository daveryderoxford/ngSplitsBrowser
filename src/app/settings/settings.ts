export interface Settings {
  statsChartSize: number;
  graphLabelTextSize: number;
  // Add other settings here as they are introduced
}

export const DEFAULT_SETTINGS: Settings = {
  statsChartSize: 500, // Example default value
  graphLabelTextSize: 12, // Example default value
};